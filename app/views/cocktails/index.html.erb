<h1>Check our drinks!</h1>
<ul>
  <% @cocktails.each do |c| %>
  <li><%= link_to c.name, cocktail_path(c) %> </li>
  <% end %>
</ul>

<%= link_to "Add a cocktail", new_cocktail_path, class: "btn btn-primary btn-lg" %>